/**
 * Skyfall Ledger — Border tokens
 *
 * 1px is the default everywhere. Use whitespace before reaching for a thicker rule.
 */
export const borderWidth = {
  none: '0',
  hairline: '1px',
  thick: '2px',
} as const;

export const borderStyle = {
  solid: 'solid',
  dashed: 'dashed',
  dotted: 'dotted',
} as const;
